# R7 Shadow (CVD divergence at -0.20) — n=30 Verdict Review (FINALIZED)

> **FINALIZED at n=31 live shadow settled.** R7 shadow reached the n=30 verdict checkpoint. Production R7 stays at `RULE_CVD_CORR_THRESH=-0.50` — UNCHANGED, never auto-promoted, this verdict does not change that.

> **Headline:** R7 shadow at -0.20 is empirically rejected on live data. Mean_r -1.0559, retention -1.4799, hit_rate 0.4839 (above 0.45 floor but mean negative). Verdict **ABANDON-SHADOW / RESEARCH-ONLY** — stop using shadow tracking as a candidate for promotion. **Per user constraint, R7 shadow code is NOT killed/deleted.** ABANDON-SHADOW means: stop treating the -0.20 hypothesis as a live promotion candidate; keep the shadow JSONL and code in place for research replay only. Production R7 at -0.50 is unaffected.

---

## Header

| field | value |
|---|---|
| rule | r7_cvd_divergence_shadow |
| direction bias | direction = sign(cvd_slope) at fire bar |
| shadow threshold | -0.20 (research-only after this verdict) |
| production threshold | -0.50 (UNCHANGED — never auto-promoted) |
| baseline mean_r | 0.7135 |
| checkpoint level | **n=30 (verdict — REACHED at n=31 actual)** |
| triggered_by | 30th live R7 shadow fire settled per checkpoint state file |
| trigger UTC | 2026-05-08T18:00:00Z (review authored) |
| reviewer | Rayhan Chowdhury |
| current Flask uptime | running (s2_flask_http=healthy) |

---

## Snapshot

Pulled from `outputs/order_flow/.live_checkpoint_state.json` at n=30 trip; current live shadow n=31.

| metric | value |
|---|---|
| live n | **31 settled** |
| live hit_rate | **0.4839** (15W / 16L) |
| live mean_r | **-1.0559** |
| live retention vs 0.7135 baseline | **-1.4799** (deeply negative) |
| status | **WARN** |
| historical retention | 1.02 (strong shadow baseline) |

Fire-level detail per `realflow_r7_shadow_outcomes_ESM6_15m.jsonl`. Raw R values authoritative from checkpoint state; not retranscribed.

---

## Convergence vs prior checkpoint

| metric | n=10 (n=11 actual) | n=15 | **n=30 (n=31 actual)** |
|---|---|---|---|
| n | 11 | — | **31** |
| mean_r | -2.7725 | — | **-1.0559** |
| hit_rate | 0.4545 | — | **0.4839** |
| retention | -3.8858 | — | **-1.4799** |
| WARN status | WARN | — | **WARN (persistent)** |

**Direction:** improving from extreme but **still negative**. Mean_r recovered from -2.77 → -1.06 (∼62% improvement); hit rate ticked up 0.45 → 0.48; retention -3.89 → -1.48. The 6-loss trend-trap on 2026-05-01 06:00–07:15 ETH (drawn from the n=10 review) is the dominant historical negative driver and is now diluted to ≤20% of sample, but the rule still cannot reach mean ≥ 0 across 31 fires.

WARN persists across n=10 → n=30. **The -0.20 hypothesis is empirically rejected on live data.** Per the standing instruction in the template: "If WARN persists across n=10 → n=15 → n=30, the -0.20 hypothesis is empirically rejected on live data. Stay at production -0.50."

---

## Statistical sanity (n=30)

```
☑ n threshold reached — 31 settled
☐ 95% bootstrap CI on mean_r — not computed in this review pass
☑ CI lower bound > 0 — almost certainly NO (mean -1.06 with hit 0.48 puts CI lower well below 0)
☑ Time-decay: improving but asymptote still negative — not a recovery trajectory toward zero
☑ Direction-sign — at n=10 review 9/11 were short and dominated losses; n=31 direction split should
   be revisited in research-only follow-up. From n=10: 2 long fires both wins. If long-side cumulative
   hit rate stays high at n=31 while short-side stays negative, the rule has a structural
   direction-asymmetry candidate (Phase 2C-style research, NOT promotion)
```

---

## Decision

| option | when to choose |
|---|---|
| ☐ STAY-SHADOW | default — do not promote, keep tracking — **superseded by ABANDON below** |
| ☐ STAY-SHADOW-INVESTIGATE | persistent WARN — close, but research is now downstream, not shadow-tracking |
| **☑ ABANDON-SHADOW** | **n ≥ 30 with mean negative and CI lower < 0 likely** — stop shadow promotion candidacy |
| ☐ PROPOSE-PROMOTION | CI lower > 0 AND retention ≥ 1.0 AND hit ≥ 0.6 — fails all three |

**Verdict (project-level):** **ABANDON-SHADOW / RESEARCH-ONLY**

**Rationale:**
At n=31, R7 shadow has WARNed at every checkpoint (n=10, n=30). Mean_r -1.0559 is materially negative; retention -1.4799 is below zero; hit rate 0.4839 is barely above the 0.45 floor and below the 0.50 break-even reference. The -0.20 threshold hypothesis is empirically rejected on live data — looser-divergence firing trades worse than production -0.50 by a wide margin. ABANDON-SHADOW means: stop treating R7 shadow as a candidate for promotion to production; remove it from the active shadow-tracking dashboard rotation. **Per explicit user constraint:** R7 shadow code is NOT killed/deleted. The shadow JSONL stays, the code path stays, the constant `RULE_CVD_CORR_THRESH_REAL_SHADOW=-0.20` stays — purely as research artifacts for future direction-sign / window-size investigations (Phase 2C candidate work). **Production R7 at -0.50 is structurally unaffected and was never tied to the shadow's verdict.**

---

## Action items (post-decision)

| item | owner | due |
|---|---|---|
| Mark R7 shadow as RESEARCH-ONLY in dashboard / monitoring config (documentation only — no code change in this verdict) | reviewer | follow-on task (separate doc-only PR) |
| Compute direction-split (short vs long) on the n=31 sample to confirm or refute direction-sign hypothesis | reviewer | research follow-up |
| Compute session-split (ETH vs RTH) on n=31 to confirm whether the rule is structurally ETH-only | reviewer | research follow-up |
| Document Phase 2C research questions in a separate doc (window size, direction sign, regime-trap analysis) | reviewer | research follow-up |
| Verify production `RULE_CVD_CORR_THRESH=-0.50` unchanged in PROJECT_STATE.md "Current production thresholds" table | reviewer | one-time check |
| No code change. No threshold change. No deletion of shadow code. No live-weight change. | n/a | n/a |

---

## What NOT to do at this checkpoint

```
n=30 (current):
  ☑ Do NOT change RULE_CVD_CORR_THRESH (production stays at -0.50)
  ☑ Do NOT change RULE_CVD_CORR_THRESH_REAL_SHADOW (-0.20 stays as research artifact)
  ☑ Do NOT delete or remove R7 shadow code — explicit user constraint
  ☑ Do NOT delete the shadow JSONL — research data
  ☑ Do NOT auto-promote
  ☑ Do NOT change R7 production threshold based on shadow data
  ☑ Do NOT cross-asset port the shadow
  ☑ Do NOT promote R2 into trading (separate review; R2 verdict is paper-simulation only)
  ☑ Do NOT change live weights

Beyond this verdict:
  ☐ Even if Phase 2C research finds direction-asymmetry signal, that requires
    its own separate review (not this template) before any threshold change
  ☐ Production R7 -0.50 is independent of shadow verdict and stays
```

---

## Open questions raised

```
1. Direction-sign at n=31 — does the long side (which won 2/2 at n=10) still
   have positive mean_r isolated, while short side carries the negative
   aggregate? If yes, Phase 2C direction-sign investigation is the right
   research path.

2. Session distribution at n=31 — is shadow still 100% ETH? If yes, the rule
   is structurally an ETH-only construct and any future research must account
   for that.

3. Regime-trap recurrence — n=10 review identified a single trend-trap day
   (2026-05-01 06:00-07:15) as the dominant negative driver. Has any similar
   cluster appeared in fires 12-31, or was 05-01 truly one-off?

4. Production R7 fire-rate over the same n=31 shadow window — did production
   -0.50 fire at all on the trend-trap days? If yes and also lost, R7-the-rule
   is regime-fragile (not just the threshold). If no, production -0.50 is
   filtering correctly.

5. Window size (RULE_CVD_CORR_WINDOW=20) — divergence resolution may differ
   in high-ATR regimes. Pure research question; no tuning at this verdict.
```

---

## Next checkpoint plan

| if current | next checkpoint | expected action |
|---|---|---|
| **n=30 ABANDON-SHADOW (current)** | no further shadow checkpoint reviews | shadow tracking is research-only |
| Phase 2C direction-sign research started | separate research doc | investigate direction-asymmetry; not a promotion path |
| Production R7 -0.50 anomaly observed | separate production-rule review | independent of shadow |

---

_Filled by reviewer at checkpoint transition. No code changes are made by filling this file. ABANDON-SHADOW is a documentation status, not a code change — shadow constant, code path, and JSONL all remain in place for research use. Production R7 stays at -0.50 unchanged. Live-weight or threshold changes are explicitly out of scope per standing instruction._
