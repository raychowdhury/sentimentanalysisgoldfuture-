# R1 (fade buyer dominance) — n=30 Verdict Review (FINALIZED)

> **FINALIZED at n=30 live settled.** R1 reached the n=30 verdict checkpoint per `outputs/order_flow/.live_checkpoint_state.json`. This is the verdict-level review for R1; no config, threshold, or production behavior is changed by filling this file (per standing instruction).

> **Headline:** mean_r remains positive (+0.5331R) but retention has deteriorated below the 0.8 KEEP gate (0.4518). Hit rate 0.6333 still strong. Status WARN. Verdict **KEEP-WATCH** — short of REVERT (mean still positive), short of full KEEP (retention failed).

---

## Header

| field | value |
|---|---|
| rule | r1_buyer_down |
| direction bias | short |
| baseline mean_r | 1.18 |
| checkpoint level | **n=30 (verdict — REACHED)** |
| triggered_by | 30th live R1 fire settled per checkpoint state file |
| trigger UTC | 2026-05-08T18:00:00Z (review authored) |
| reviewer | Rayhan Chowdhury |
| current Flask uptime | running (s2_flask_http=healthy) |
| current live SDK status | streaming |

---

## Snapshot

Pulled from `outputs/order_flow/.live_checkpoint_state.json` at n=30 trip.

| metric | value |
|---|---|
| live n | **30 settled** |
| live hit_rate | **0.6333** (19W / 11L) |
| live mean_r | **+0.5331** |
| live retention vs 1.18 baseline | **+0.4518** (below 0.8 KEEP gate) |
| status | **WARN** |
| historical retention | 0.07 (known very weak) |

Fire-level detail per `realflow_outcomes_ESM6_15m.jsonl` (mode=live). Raw R values authoritative from checkpoint state; not retranscribed in this verdict log.

---

## Convergence vs prior checkpoint

| metric | n=10 (n=11 actual) | n=15 | **n=30 (current)** |
|---|---|---|---|
| n | 11 | — | **30** |
| mean_r | +0.7858 | — | **+0.5331** |
| hit_rate | 0.8182 | — | **0.6333** |
| retention | 0.666 | — | **0.4518** |
| WARN status | OK (KEEP-WITH-CAVEAT) | — | **WARN** |

**Direction:** convergent-downward. Mean_r ↓ 32% (0.79 → 0.53), hit ↓ 23% (0.82 → 0.63), retention ↓ 32% (0.67 → 0.45). Trajectory predicted at n=10 (cluster-fragility flag) materialized. Two outlier losses noted at n=10 (MAE -6.16R, -12.01R on 2026-05-01 10:30/11:00) likely still anchoring mean downward; 19 new fires (n=11 → n=30) net average ≈ +0.41R.

R1 historical retention is 0.07. Live retention 0.4518 = **6.5× historical baseline** despite gate failure. The rule is genuinely above its prior backtest, but well below the gate set for tradability.

---

## Statistical sanity (n=30)

```
☑ n threshold reached — 30 settled
☐ 95% bootstrap CI on mean_r — not computed in this review pass (deferred)
☐ CI lower bound > 0 — undetermined; mean +0.53 with hit 0.63 suggests CI lower likely > 0 but not verified
☐ Time-decay: rolling 10-bar trend — qualitative; new-fire mean ~+0.41R is below early-fire mean ~+0.79R
☐ Direction-sign — all R1 fires direction=-1 (short) by design; sign correctness consistent
```

Bootstrap CI computation deferred — not blocking the verdict. The KEEP-WATCH decision rests on the deterministic metric gates, not CI.

---

## Decision

| option | when to choose |
|---|---|
| ☐ KEEP | CI lower > 0 AND retention ≥ 0.8 AND hit ≥ 0.55 — **fails retention gate (0.45 < 0.8)** |
| ☐ KEEP-WITH-CAVEAT | CI lower > 0 BUT retention 0.5-0.8 — **fails (retention 0.45 < 0.5)** |
| ☐ REVERT | mean_r ≤ 0 — **not applicable (mean +0.53 positive)** |
| ☐ RETUNE | one session breaking it; document but do NOT change config — not the dominant failure |
| ☐ INVESTIGATE | ambiguous — partial fit |
| **☑ KEEP-WATCH** | **(custom — between KEEP-WITH-CAVEAT and REVERT)** mean still positive but retention below 0.5 floor; rule retains some live edge but materially below promotability bar. Do not revert; do not promote. Continue paper-tracking; revisit with bootstrap CI at n=50 / n=60. |

**Selected:** **KEEP-WATCH**

**Rationale:**
At n=30, R1 mean_r +0.5331 is positive but retention 0.4518 fails both the 0.8 KEEP gate and the 0.5 KEEP-WITH-CAVEAT floor. Hit rate 0.6333 is still well above the 0.55 hit floor. The convergence is downward across all three core metrics from n=10 → n=30; new-fire performance (n=11 → n=30) is materially weaker than early-fire performance, consistent with the n=10 caveat that early results were outlier-compressed. R1 is **live-edge-positive but below tradability bar.** Per user instruction this verdict log is the only artifact: no thresholds change, R1 production firing continues, no R7 promotion, no live-weight allocation. Re-evaluate at n=50 to determine whether deceleration continues toward zero (REVERT territory) or stabilizes near baseline 0.07 retention (effectively unchanged from historical — REVERT-equivalent).

---

## Action items (post-decision)

| item | owner | due |
|---|---|---|
| Compute 95% bootstrap CI on the 30 R values; record lower bound | reviewer | next review pass |
| Tag fires 11–30 in JSONL for the new-fire window (n=11 → n=30) and compute their isolated mean_r / hit / retention | reviewer | n=50 prep |
| Watch retention trajectory: if drops below 0.30 by n=50, escalate to REVERT pre-decision | reviewer | continuous |
| If session breakdown shows RTH coverage is now non-zero, document RTH-only mean_r vs ETH-only | reviewer | n=50 |
| No code change. No threshold change. No live-weight change. No R1 production behavior change. | n/a | n/a |

---

## What NOT to do at this checkpoint

```
n=30 (current):
  ☑ Do NOT auto-revert (env flag flip is manual; mean_r still positive — REVERT not warranted)
  ☑ Do NOT change R1 thresholds individually (revert is all-or-nothing if ever applied)
  ☑ Do NOT promote R1 into trading weights — verdict is sub-KEEP
  ☑ Do NOT promote R7 shadow (separate review)
  ☑ Do NOT kill / delete R7 code (R7 production stays at -0.50 unchanged)
  ☑ Do NOT cross-asset port
  ☑ Do NOT change R2 promotion status here (R2 has its own verdict review)
```

---

## Open questions raised

```
1. Why did retention deteriorate from 0.67 (n=10) → 0.45 (n=30) while mean stayed
   positive? Outlier-compression at n=10 partially explains, but new-fire mean
   ~+0.41R is also weaker than the early-fire mean. Regime decay or sample
   reversion to the historical baseline (0.07)?

2. Is retention 0.4518 "live-positive but sub-tradable" stable, or is it
   trending toward zero? n=50 needed to disambiguate.

3. RTH coverage status at n=30? n=10 review flagged 100% ETH; if n=30 still
   ETH-only, generalisation gap remains the dominant unresolved risk.

4. Bootstrap CI lower bound — is it actually > 0? Mean +0.53 with hit 0.63
   suggests probably yes, but not verified; required before any future
   promotion conversation.
```

---

## Next checkpoint plan

| if current | next checkpoint | expected action |
|---|---|---|
| **n=30 KEEP-WATCH (current)** | n=50 mid-probe | re-review with bootstrap CI; check whether retention stabilizes or decays |
| n=50 retention ≥ 0.5 + CI lower > 0 | n=60 verdict | reconsider KEEP-WITH-CAVEAT |
| n=50 retention < 0.30 OR mean turns ≤ 0 | escalate to REVERT pre-decision | document; manual env flag flip is separate process |
| n=50 KEEP-WATCH persists | continue paper-tracking only | no live-weight allocation |

---

_Filled by reviewer at checkpoint transition. No code changes are made by filling this file. Decision implementation (any threshold or live-weight change) is explicitly out of scope per standing instruction._
