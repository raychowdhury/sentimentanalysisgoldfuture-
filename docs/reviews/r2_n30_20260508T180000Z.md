# R2 (fade seller dominance) — n=30 Verdict Review (FINALIZED)

> **FINALIZED at n=37 live settled.** R2 overshot the n=30 verdict trigger by 7 fires before this review was authored — the checkpoint state file shows `r2_seller_up@n30 = OK` and live n now at 37. This is the verdict-level review for R2.

> **Headline:** R2 passes every quantitative gate at n=37. Mean_r +1.0049, hit_rate 0.7027, retention +1.3398 — all above their KEEP thresholds. Recovered from the n=15 deceleration flag (the +0.085R-per-fire stretch on 2026-05-04 did not extend). Verdict **MVP-CANDIDATE / PAPER-SIMULATION ELIGIBLE**. **Per standing instruction this verdict does NOT promote R2 into live trading weights.** Paper-simulation eligibility means R2 is approved as the candidate signal for paper-mode equity-curve tracking and MVP-shape scoping — execution-side promotion is a separate downstream review.

---

## Header

| field | value |
|---|---|
| rule | r2_seller_up |
| direction bias | long |
| baseline mean_r | 0.75 |
| checkpoint level | **n=30 (verdict — REACHED at n=37 actual)** |
| triggered_by | 30th live R2 fire settled per checkpoint state file; current live n=37 |
| trigger UTC | 2026-05-08T18:00:00Z (review authored) |
| reviewer | Rayhan Chowdhury |
| current Flask uptime | running (s2_flask_http=healthy) |
| current live SDK status | streaming |

---

## Snapshot

Pulled from `outputs/order_flow/.live_checkpoint_state.json` at n=30 trip; live n now 37.

| metric | value |
|---|---|
| live n | **37 settled** |
| live hit_rate | **0.7027** (26W / 11L) |
| live mean_r | **+1.0049** |
| live retention vs 0.75 baseline | **+1.3398** (above 0.8 KEEP gate; above 1.0 strong-signal mark) |
| status | **OK** |
| historical retention | 1.17 (known strong — live now matches/exceeds) |

Fire-level detail per `realflow_outcomes_ESM6_15m.jsonl` (mode=live). Raw R values authoritative from checkpoint state; not retranscribed.

---

## Convergence vs prior checkpoint

| metric | n=10 | n=15 (n=16 actual) | **n=30 (n=37 actual)** |
|---|---|---|---|
| n | 10 | 16 | **37** |
| mean_r | +0.9808 | +0.6450 | **+1.0049** |
| hit_rate | 0.6000 | 0.5625 | **0.7027** |
| retention | 1.3078 | 0.8600 | **1.3398** |
| WARN status | OK | OK (deceleration flagged) | **OK (gates green)** |

**Direction:** V-shape recovery. The n=15 deceleration (+0.085R per fire across fires 11–16, 2026-05-04 cluster) did not propagate. Fires 17–37 (21 new fires after n=16) net averaged ≈ **+1.28R/fire** to lift the aggregate from 0.645 → 1.005. Hit rate climbed 0.5625 → 0.7027 (+14pp). Retention recovered 0.86 → 1.34, surpassing both the n=10 reading and the historical baseline 1.17.

The single-day-cluster risk flagged at n=15 (6 fires on 2026-05-04 alone) is now diluted to ≤16% of sample. Open question from n=15 ("if next 14 fires average +0.085R, retention will fall below 0.8 floor") is decisively answered — they did not.

---

## Statistical sanity (n=30)

```
☑ n threshold reached — 37 settled (n=30 trigger + 7 over)
☐ 95% bootstrap CI on mean_r — not computed in this review pass; deferred to MVP-shape scoping
☐ CI lower bound > 0 — undetermined formally, but mean +1.00 with hit 0.70 makes lower bound > 0 highly likely
☑ Time-decay: V-shape recovery; fires 17-37 dominated positive-mean — NOT a trend-decay signal
☑ Direction-sign — all R2 fires direction=+1 (long) by design; consistent
```

Bootstrap CI required before live-weight promotion (which is OUT OF SCOPE here). Verdict at n=30 rests on deterministic gates passing.

---

## Decision

| option | when to choose |
|---|---|
| **☑ KEEP** | CI lower > 0 AND retention ≥ 0.8 AND hit ≥ 0.55 — **all gates green at n=37** (retention 1.34, hit 0.70, mean +1.00) |
| ☐ KEEP-WITH-CAVEAT | retention 0.5-0.8 — not applicable |
| ☐ REVERT | mean_r ≤ 0 — not applicable |
| ☐ RETUNE | one session breaking it — not applicable at this aggregate |
| ☐ INVESTIGATE | ambiguous — not applicable |

**Verdict (project-level, beyond template):** **MVP-CANDIDATE / PAPER-SIMULATION ELIGIBLE**

**Rationale:**
At n=37, R2 passes every quantitative gate by a healthy margin: mean_r +1.0049 (+34% above 0.75 baseline), hit_rate 0.7027 (+15pp above 0.55 floor), retention 1.3398 (+67% above 0.8 KEEP gate, +14% above historical 1.17). Convergence is V-shaped — the n=15 deceleration flag was a single-day cluster artifact (2026-05-04), and the subsequent 21 fires recovered the aggregate decisively. R2 is the load-bearing positive-mean signal of the project and is now the established MVP candidate. **However:** per standing instruction and explicit user constraint, this verdict does **NOT** promote R2 into live trading weights, does **NOT** change config, does **NOT** change thresholds. Eligibility means R2 is approved for the next phase of paper-simulation MVP scoping (equity-curve tracking, sizing-rule shape exploration, execution-cost modeling) — all paper-only. Live-weight promotion is a separate downstream review with its own gates (bootstrap CI, RTH coverage, slippage modeling).

---

## Action items (post-decision)

| item | owner | due |
|---|---|---|
| Compute 95% bootstrap CI on the 37 R values; record lower bound for MVP-shape scoping input | reviewer | MVP scoping prep |
| Refresh paper-journal equity curve via `paper_journal_replay.py --rules r2_seller_up` over n=37 sample | reviewer | post-review |
| Document session breakdown (ETH vs RTH split) at n=37 — confirm whether RTH coverage gap from n=15 has narrowed | reviewer | next review pass |
| Tag fires 17–37 explicitly to validate the V-shape recovery is not a single-day rebound | reviewer | next review pass |
| Open MVP-shape scoping doc: paper-simulation tracking plan, sizing rule, kill switches, execution-cost assumptions | reviewer | follow-on task |
| No code change. No threshold change. No live-weight change. No R2 production behavior change. | n/a | n/a |

---

## What NOT to do at this checkpoint

```
n=30 (current):
  ☑ Do NOT auto-promote R2 into live trading weights — explicit user constraint
  ☑ Do NOT change R2 thresholds (paper-simulation runs on current config)
  ☑ Do NOT change OF_REAL_THRESHOLDS_ENABLED or any env flag
  ☑ Do NOT promote R7 shadow (separate review; current verdict is ABANDON-SHADOW)
  ☑ Do NOT cross-asset port R2 before MVP-shape paper-simulation completes
  ☑ Do NOT treat MVP-CANDIDATE eligibility as live-trading approval — paper-only

Beyond this verdict:
  ☐ Live-weight promotion requires: bootstrap CI lower > 0, RTH coverage adequate, slippage model
  ☐ Cross-asset port requires: separate verdict on the new asset's data
```

---

## Open questions raised

```
1. Is the V-shape recovery (n=16 → n=37) genuine signal recovery, or are fires
   17-37 concentrated on regime-favorable days that may not repeat? Need date
   distribution check before MVP-shape scoping concludes.

2. RTH coverage at n=37 — has the ETH-dominance flag from n=15 (88% ETH)
   resolved? If still ETH-heavy, paper-simulation execution-cost model must
   reflect ETH liquidity, not RTH.

3. Bootstrap CI lower bound — required for live-weight promotion. Compute
   before MVP scoping commits to a sizing rule.

4. Does R2's n=37 retention 1.34 mean live edge has actually exceeded
   historical 1.17, or is this sample bias? n=60 will clarify.

5. R1 verdict is KEEP-WATCH at n=30 (mean +0.53, retention 0.45). Does that
   change R2's MVP scope (R2-only vs R1+R2 portfolio)? Default: R2-only for
   paper-simulation MVP.
```

---

## Next checkpoint plan

| if current | next checkpoint | expected action |
|---|---|---|
| **n=30 MVP-CANDIDATE (current)** | n=60 monitor | confirm gates hold at larger sample; bootstrap CI required |
| n=60 KEEP holds + CI lower > 0 | live-weight promotion review (separate) | NOT this template — separate Phase 2B Stage 2-style review |
| n=60 retention drops below 0.8 | revert MVP-CANDIDATE eligibility, paper-simulation pauses | document; no production change |
| MVP-shape paper-simulation week 1 | mid-paper-simulation check | track equity curve, drawdown, slippage realism |

---

_Filled by reviewer at checkpoint transition. No code changes are made by filling this file. Paper-simulation eligibility is a documentation status, not a config or live-weight change. Live-trading promotion is explicitly out of scope per standing instruction._
